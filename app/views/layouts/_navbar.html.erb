<nav class="navbar navbar-expand-lg navbar-light bg-white nav-bottom">
  <h4 class="pt-2">Dev-<strong class="logo">BLOG</strong></h4>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav font-size home-links">
     <% if user_signed_in? %>
      <li class="nav-item" >
        <%= link_to 'Home', root_path, class: "#{cp(root_path)} nav-link font-weight-bold"%>
      </li>
      <li class="nav-item">
        <%= link_to 'Create an Article', new_article_path, class: "#{cp(new_article_path)} nav-link font-weight-bold" %>
      </li>
        <%= render 'categories/categories'%>
      <li class="nav-item"></li>
       <% end %>
    </ul>
    <ul class="navbar-nav font-size ml-auto search-position">
      <% if user_signed_in? %>
        <li class='search-form' ><%= render 'layouts/search_form' %></li>
      <% end %>
     <li><%= render 'layouts/login'%></li>
    </ul>
  </div>
</nav>
