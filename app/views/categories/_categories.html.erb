<% Category.all.each do |category|%>
  <li class="nav-item">
    <li><%= link_to category.name, category_path(category.id), class: "#{cp(category_path(category.id))} nav-link font-weight-bold" %></li>
  </li>
<% end %>