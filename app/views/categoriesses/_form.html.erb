<%= form_with(model: categoriess, local: true) do |form| %>
  <% if categoriess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoriess.errors.count, "error") %> prohibited this categoriess from being saved:</h2>

      <ul>
        <% categoriess.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
